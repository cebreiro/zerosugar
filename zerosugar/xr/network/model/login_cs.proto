syntax = "proto3";

import "option.proto";

package zerosugar.xr.network.login.cs;

message CreateAccount
{
    option (opcode) = 0;

    string account = 1;
    string password = 2;
}

message Login
{
    option (opcode) = 1;

    string account = 1;
    string password = 2;
}
